package net.aetherealtech.ujpeinvoice.internal.json;

/** The JSON {@code null} literal. A singleton enum rather than a record — there is only ever one. */
public enum JsonNull implements JsonValue {
    INSTANCE
}
