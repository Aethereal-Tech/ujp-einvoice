The instructions for any agent working in this repository are in `CLAUDE.md`: the rules that bind every change, the
one hard rule on `@ProvisionalSpec`, and the coverage gates. `openspec/specs/` is the record of what is built and
`openspec/changes/` what is planned. Read `CLAUDE.md` and the specs your change touches before touching anything;
`CLAUDE.md` wins where they seem to differ.
